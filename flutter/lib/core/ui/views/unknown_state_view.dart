import 'package:flutter/material.dart';

class UnknownStateView extends StatelessWidget {
  const UnknownStateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Unknown state view.'),);
  }
}